# MIDical Lab — Sample Reports

Static HTML samples from the MIDical Lab experiment, hosted via GitHub Pages.

## Live links

- **Landing page** — `https://christ-flex.github.io/midical-lab-reports/`
- **Weekly report** — `https://christ-flex.github.io/midical-lab-reports/weekly.html`
- **Monthly deep dive** — `https://christ-flex.github.io/midical-lab-reports/monthly.html`
- **Decision-tree walkthrough** — `https://christ-flex.github.io/midical-lab-reports/walkthrough.html`

## What's in here

| File | Source |
|---|---|
| `index.html` | Landing page with links to the three reports |
| `weekly.html` | Generated by `synthesize_weekly_and_monthly.py` (mirrors `MIDicalLab_sample_weekly_report_<caid>.html`) |
| `monthly.html` | Generated by `synthesize_weekly_and_monthly.py` (mirrors `MIDicalLab_sample_monthly_deep_dive_<caid>.html`) |
| `walkthrough.html` | Generated by `synthesize_weekly_and_monthly.py` (mirrors `MIDicalLab_sample_decision_walkthrough_<caid>.html`) |

## How to update

Re-run `synthesize_weekly_and_monthly.py` from the Flex Toolkit project — it
writes to `docs/sample_reports/` and also mirrors the three reports here with
clean filenames. Then commit and push:

```bash
git add weekly.html monthly.html walkthrough.html
git commit -m "Refresh sample reports"
git push
```

GitHub Pages picks up the new files within ~1 minute.

## About the data

Samples are generated against live CAID 937700006246 (a FlexFactor merchant
CAID) as a structural reference. Production reports point at the two experiment
MIDs (LOW 937700011015 / MED 937700011639) and regenerate every Monday.

The HTML files are self-contained — embedded CSS, Inter from Google Fonts with
system-sans fallback, no external assets or JavaScript. Inline SVG for all
charts and sparklines.
